{
  "name": "my-portfolio",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "astro build && touch dist/CNAME && echo 'annahyatt.com' > dist/CNAME && gh-pages -d dist -b gh-pages --dotfiles"
  },
  "dependencies": {
    "@astrojs/mdx": "^4.1.0",
    "@fontsource/ibm-plex-mono": "^5.2.5",
    "@fontsource/ibm-plex-sans": "^5.1.1",
    "@fontsource/inter": "^5.1.1",
    "astro": "^5.3.0"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}

